// for (var s = x; x <= 255; x++) {
//     if (x < 255) {
//         ofijweeo.send("bright" = x)
//     };
//     else if (x == 255) {
//         while (ture) {
//             forwoifjweoif.send("bright" = x)
//         }
//     };

// }
let i = 1;  // 초기화
while (i <= 5) { // 조건문
    const text = i + "번 째 반복문<br>";
    document.write(text);

    i++; // 증감식
}


if (x == 255) {
    while (ture) {
        slifejslfj.sedn(wofjweoifj);
    }
}


let i = 1;  // 초기화
while (true) { // 조건문
    const text = i + "번 째 반복문<br>";
    document.write(text);
    i++;
    if (i == 50) {
        while (true) {
            const text = i + "번 째 반복문<br>";
            document.write(text);
        }
    }
}


let i = 1;  // 초기화
while(true)  { // 조건문
  const text = i + "번 째 반복문<br>"; 
  document.write(text); //이 부분에 라이트 들어오는 명령
 i++;
  if(i==255){
    while(true){
      const text = i + "번 째 반복문<br>";
    // 여기서 if 문으로 break 걸어서 멈추게할수 있음
  document.write(text);  //이 부분에 라이트 들어오는 명령
    }
      }
    }
